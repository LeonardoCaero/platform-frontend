# API base URL — used by axios at build time (Vite bakes it in)
# For local dev, the default fallback in axios.ts is http://localhost:4000/api
VITE_API_URL=http://localhost:4000/api

# Web Push — must match VAPID_PUBLIC_KEY in platform-api .env
VITE_VAPID_PUBLIC_KEY=your_vapid_public_key_here
